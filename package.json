{
  "name": "forge",
  "private": true,
  "version": "1.0.0",
  "description": "Tyler Forge™ Monorepo",
  "scripts": {
    "prepare": "husky",
    "dev:forge": "pnpm run --filter @tylertech/forge dev",
    "storybook:forge": "pnpm run --filter @tylertech/forge storybook",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "format": "turbo run format",
    "format:check": "turbo run format:check",
    "test": "turbo run test",
    "release": "pnpm run build && changeset publish"
  },
  "keywords": [
    "tyler forge"
  ],
  "author": "Tyler Technologies, Inc.",
  "license": "Apache-2.0",
  "packageManager": "pnpm@10.28.2",
  "engines": {
    "node": ">=18",
    "pnpm": ">=10"
  },
  "pnpm": {
    "patchedDependencies": {
      "custom-element-vs-code-integration@1.5.0": "patches/custom-element-vs-code-integration@1.5.0.patch",
      "@changesets/assemble-release-plan@6.0.9": "patches/@changesets__assemble-release-plan@6.0.9.patch"
    }
  },
  "devDependencies": {
    "@changesets/cli": "2.29.8",
    "@commitlint/cli": "^20.4.1",
    "@commitlint/config-conventional": "^20.4.1",
    "@repo/prettier-config": "workspace:^",
    "@types/node": "catalog:",
    "commitlint": "^20.4.1",
    "eslint": "catalog:",
    "husky": "^9.1.7",
    "prettier": "catalog:",
    "lint-staged": "^16.2.7",
    "turbo": "^2.8.3"
  }
}

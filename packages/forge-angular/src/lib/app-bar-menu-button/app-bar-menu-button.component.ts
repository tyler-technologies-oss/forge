// This code was generated by the angular-custom-elements-schematic.  Any changes will be overwritten next time it runs.
import { booleanAttribute, Component, ElementRef, ChangeDetectionStrategy, ChangeDetectorRef, NgZone, Input, inject } from '@angular/core';
import { AppBarMenuButtonComponent as AppBarMenuButtonComponentCustomElement, defineAppBarMenuButtonComponent } from '@tylertech/forge';

/**  */
@Component({
  selector: 'forge-app-bar-menu-button',
  changeDetection: ChangeDetectionStrategy.OnPush,
  template: '<ng-content></ng-content>',
  standalone: false
})
export class AppBarMenuButtonComponent {
  protected elementRef = inject<ElementRef<AppBarMenuButtonComponentCustomElement>>(ElementRef);
  protected zone = inject(NgZone);

  /** The forge-app-bar-menu-button element. */
  public readonly nativeElement = this.elementRef.nativeElement;

  /** The name of an alternative icon to display. */
  @Input()
  public set icon(value: AppBarMenuButtonComponentCustomElement['icon']) {
    this.zone.runOutsideAngular(() => {
      this.nativeElement.icon = value;
    });
  }
  public get icon(): AppBarMenuButtonComponentCustomElement['icon'] {
    return this.nativeElement.icon;
  }

  /** Whether the menu controlled by this button is expanded. Reflected as `aria-expanded` on the internal button. */
  @Input({ transform: booleanAttribute })
  public set expanded(value: AppBarMenuButtonComponentCustomElement['expanded']) {
    this.zone.runOutsideAngular(() => {
      this.nativeElement.expanded = value;
    });
  }
  public get expanded(): AppBarMenuButtonComponentCustomElement['expanded'] {
    return this.nativeElement.expanded;
  }

  /** The id of the element (e.g., drawer or menu) controlled by this button. Reflected as `aria-controls` on the internal button. */
  @Input()
  public set controls(value: AppBarMenuButtonComponentCustomElement['controls']) {
    this.zone.runOutsideAngular(() => {
      this.nativeElement.controls = value;
    });
  }
  public get controls(): AppBarMenuButtonComponentCustomElement['controls'] {
    return this.nativeElement.controls;
  }

  /** Indicates the button opens a menu or dialog. Valid values include "menu", "dialog", "true". Reflected as `aria-haspopup` on the internal button. */
  @Input()
  public set hasPopup(value: AppBarMenuButtonComponentCustomElement['hasPopup']) {
    this.zone.runOutsideAngular(() => {
      this.nativeElement.hasPopup = value;
    });
  }
  public get hasPopup(): AppBarMenuButtonComponentCustomElement['hasPopup'] {
    return this.nativeElement.hasPopup;
  }

  constructor() {
    defineAppBarMenuButtonComponent();
    const changeDetectorRef = inject(ChangeDetectorRef);
    changeDetectorRef.detach();
  }
}

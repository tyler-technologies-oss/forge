// This code was generated by the angular-custom-elements-schematic.  Any changes will be overwritten next time it runs.
import { Component, ElementRef, ChangeDetectionStrategy, ChangeDetectorRef, NgZone, Input, inject } from '@angular/core';
import { TimestampComponent as TimestampComponentCustomElement, defineTimestampComponent } from '@tylertech/forge';

/**  */
@Component({
  selector: 'forge-timestamp',
  changeDetection: ChangeDetectionStrategy.OnPush,
  template: '<ng-content></ng-content>',
  standalone: false
})
export class TimestampComponent {
  protected elementRef = inject<ElementRef<TimestampComponentCustomElement>>(ElementRef);
  protected zone = inject(NgZone);

  /** The forge-timestamp element. */
  public readonly nativeElement = this.elementRef.nativeElement;

  /**
   * The date/time value to display. Accepts a Date object, an ISO string, or an HTML `<time>`
   * element's `datetime` attribute value.
   */
  @Input()
  public set datetime(value: TimestampComponentCustomElement['datetime']) {
    this.zone.runOutsideAngular(() => {
      this.nativeElement.datetime = value;
    });
  }
  public get datetime(): TimestampComponentCustomElement['datetime'] {
    return this.nativeElement.datetime;
  }

  /**
   * The CLDR format string for displaying the date. Only applies when the value of `datetime` is a
   * valid Date object or date-convertible string. Any other `datetime` value will be displayed
   * as-is unless a custom element is slotted.
   */
  @Input()
  public set format(value: TimestampComponentCustomElement['format']) {
    this.zone.runOutsideAngular(() => {
      this.nativeElement.format = value;
    });
  }
  public get format(): TimestampComponentCustomElement['format'] {
    return this.nativeElement.format;
  }

  /** The locale to use for formatting. */
  @Input()
  public set locale(value: TimestampComponentCustomElement['locale']) {
    this.zone.runOutsideAngular(() => {
      this.nativeElement.locale = value;
    });
  }
  public get locale(): TimestampComponentCustomElement['locale'] {
    return this.nativeElement.locale;
  }

  /** Places a separator dot before or after the timestamp. */
  @Input()
  public set separator(value: TimestampComponentCustomElement['separator']) {
    this.zone.runOutsideAngular(() => {
      this.nativeElement.separator = value;
    });
  }
  public get separator(): TimestampComponentCustomElement['separator'] {
    return this.nativeElement.separator;
  }

  constructor() {
    defineTimestampComponent();
    const changeDetectorRef = inject(ChangeDetectorRef);
    changeDetectorRef.detach();
  }
}

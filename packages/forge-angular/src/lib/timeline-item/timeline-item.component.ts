// This code was generated by the angular-custom-elements-schematic.  Any changes will be overwritten next time it runs.
import { Component, ElementRef, ChangeDetectionStrategy, ChangeDetectorRef, NgZone, Input, inject } from '@angular/core';
import { TimelineItemComponent as TimelineItemComponentCustomElement, defineTimelineItemComponent } from '@tylertech/forge';

/**  */
@Component({
  selector: 'forge-timeline-item',
  changeDetection: ChangeDetectionStrategy.OnPush,
  template: '<ng-content></ng-content>',
  standalone: false
})
export class TimelineItemComponent {
  protected elementRef = inject<ElementRef<TimelineItemComponentCustomElement>>(ElementRef);
  protected zone = inject(NgZone);

  /** The forge-timeline-item element. */
  public readonly nativeElement = this.elementRef.nativeElement;

  /** The theme color of the marker. */
  @Input()
  public set theme(value: TimelineItemComponentCustomElement['theme']) {
    this.zone.runOutsideAngular(() => {
      this.nativeElement.theme = value;
    });
  }
  public get theme(): TimelineItemComponentCustomElement['theme'] {
    return this.nativeElement.theme;
  }

  /** Which parts of the sidebar line should appear. */
  @Input()
  public set sidebar(value: TimelineItemComponentCustomElement['sidebar']) {
    this.zone.runOutsideAngular(() => {
      this.nativeElement.sidebar = value;
    });
  }
  public get sidebar(): TimelineItemComponentCustomElement['sidebar'] {
    return this.nativeElement.sidebar;
  }

  constructor() {
    defineTimelineItemComponent();
    const changeDetectorRef = inject(ChangeDetectorRef);
    changeDetectorRef.detach();
  }
}

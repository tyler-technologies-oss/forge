import { expect } from '@esm-bundle/chai';
import { fixture, html } from '@open-wc/testing';
import { I{{pascalCase name}}Component } from './{{dashCase name}}';

import './{{dashCase name}}';

describe('{{pascalCase name}}', () => {
  it('should instantiate shadow root', async () => {
    const el = await fixture<I{{pascalCase name}}Component>(html`<forge-{{dashCase name}}></forge-{{dashCase name}}>`);

    expect(el.shadowRoot).not.to.be.null;
  });
});

import { IconRegistry } from "@tylertech/forge/icon";
import { tylIconClose } from "@tylertech/tyler-icons";
IconRegistry.define([tylIconClose]);
const openButton = document.getElementById("open-dialog-button-demo");
const dialog = document.getElementById("dialog-demo");
const closeIconButton = document.getElementById("close-dialog-icon-button-demo");
const closeButton = document.getElementById("close-dialog-button-demo");
openButton?.addEventListener("click", () => dialog.open = true);
closeIconButton?.addEventListener("click", () => dialog.open = false);
closeButton?.addEventListener("click", () => dialog.open = false);
dialog?.addEventListener("forge-dialog-close", () => dialog.open = false);

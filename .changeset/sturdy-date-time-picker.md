---
'@tylertech/forge': minor
---

feat(date-time-picker): add new `forge-date-time-picker` inline composite that combines a calendar with three time-selection modes (`single`, `range`, `slots`). Built on Lit, composes `forge-calendar` and `forge-time-picker`, form-associated via `ElementInternals`, exposes `header`/`footer`/`time-label` slots plus calendar pass-through slots, and targets WCAG 2.1 AA.

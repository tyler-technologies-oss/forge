import { COMPONENT_NAME_PREFIX } from '../../constants';

const elementName: keyof HTMLElementTagNameMap = `${COMPONENT_NAME_PREFIX}virtual-item`;

export const VIRTUAL_ITEM_CONSTANTS = {
  elementName
};

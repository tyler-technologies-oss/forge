import { COMPONENT_NAME_PREFIX } from '../../constants';

const elementName: keyof HTMLElementTagNameMap = `${COMPONENT_NAME_PREFIX}meter-group`;

export const METER_GROUP_CONSTANTS = {
  elementName
};

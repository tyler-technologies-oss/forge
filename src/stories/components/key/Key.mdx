import { Meta, Title, Canvas } from '@storybook/blocks';
import CustomArgTypes from '../../blocks/CustomArgTypes';
import * as KeyStories from './Key.stories';

<Meta of={KeyStories} />

<Title />

Keys are used to label data in a chart or visualization.

<Canvas of={KeyStories.Demo} />

## API

<CustomArgTypes />

## Accessibility

Ensure that the icon colors of the key items are easily distinguishable and match the color of the
associated visualization.

The value slot may be used to create an accessible text alternative for data in the visualization.

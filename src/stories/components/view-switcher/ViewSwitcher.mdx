import { Meta, Title, Canvas } from '@storybook/blocks';
import CustomArgTypes from '../../blocks/CustomArgTypes';
import * as ViewSwitcherStories from './ViewSwitcher.stories';

<Meta of={ViewSwitcherStories} />

<Title />

The view switcher component allows you to wrap a `<forge-view>` element around any HTML content to control transitions between adjecent content. By default the view switcher is set to no transition, with options to either `fade` or `slide` as a transition.

>**Note**: Content hidden by this component is still present in the DOM unless you control it manually.

<Canvas of={ViewSwitcherStories.Demo} />

## API

<CustomArgTypes />

## Accessibility
- Verify that you can tab into the `<forge-view>` content.  
- Verify that you can access controls withing the `<forge-view>` content using only the keyboard.
- You should ensure that you have the correct hierarchy of header tags. ie: `<h1>`, `<h2>`, `<h3>`, etc...
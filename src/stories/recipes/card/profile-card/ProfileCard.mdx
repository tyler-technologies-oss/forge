import { Meta, Title, Canvas } from '@storybook/blocks';
import CustomArgTypes from '../../../blocks/CustomArgTypes';
import * as ProfileStories from './ProfileCard.stories';

<Meta of={ProfileStories} />

# Recipe: Profile

This recipe shows an example of using a card to show user/profile information.

<Canvas of={ProfileStories.Demo} />

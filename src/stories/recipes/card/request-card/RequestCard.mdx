import { Meta, Title, Canvas } from '@storybook/blocks';
import CustomArgTypes from '../../../blocks/CustomArgTypes';
import * as RequestCardStories from './RequestCard.stories';

<Meta of={RequestCardStories} />

# Recipe: Request Card

This "request card" recipe is an example of how to create a styled card that offers a common internal layout to represent a how to
group related information together with optional actions.

<Canvas of={RequestCardStories.Demo} />

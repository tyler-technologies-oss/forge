import { DefaultTemplate as IconButtonDemo } from './templates/default';
import { IconButtonDefaultCodeHtml } from './code/icon-button-default';
import { IconButtonToggleCodeHtml } from './code/icon-button-toggle';

<PageSection>

## Usage

The icon-button component is nothing more than a standard HTML `<button>` with custom styling provided. Use this component to display an interactive button that renders **only an icon**. You can also use this component to toggle between an "on" and "off" state as well.

> Ensure that you add an `aria-label` attribute to the `<button>` element to ensure that the action is announced properly to screen readers.
>
> Icon buttons use icons from the [Tyler icon library](https://forge.tylertech.com/core-components/iconography/library).

</PageSection>

---

<PageSection>

## Example

### Common

<LiveDemo 
dense 
codeHtml={IconButtonDefaultCodeHtml()}>
  <IconButtonDemo/>
</LiveDemo>

### Toggle

Use the toggle variant to swap between and "on" and "off" state when clicking the button.

Valid icon tags: `<i>`, `<span>`, `<svg>`, `<img>`

<LiveDemo 
dense 
codeHtml={IconButtonToggleCodeHtml()}>
  <IconButtonDemo toggle={true}/>
</LiveDemo>

</PageSection>

---

<PageSection>

## Properties/Attributes

<PropertyDef name="toggle" type="boolean" defaultValue="false">

Controls whether the component allows for toggling the icon.
  
</PropertyDef>

<PropertyDef name="isOn" type="boolean" defaultValue="false">

Gets/sets whether the toggle is "on" or "off".
  
</PropertyDef>

<PropertyDef name="dense" type="boolean" defaultValue="null">

Gets/sets the dense state.
  
</PropertyDef>

<PropertyDef name="densityLevel" type="number" defaultValue="5">

Gets/sets the density level. Valid values: `1` (least dense) - `6` (most dense)
  
</PropertyDef>

</PageSection>

---

<PageSection>

## Methods

<MethodDef name="layout(): void;">

Re-renders the component. Use this after applying any transformations that may cause the ripple to be misaligned (**note:** dialog can cause this).

</MethodDef>

</PageSection>

---

<PageSection>

## Events

| Name                                | Description
| :-----------------------------------| :------------------
| `forge-icon-button-change`            | Emits a `CustomEvent` only when `toggle` equals `true`. Event `detail` contains a boolean value of whether the button is toggled on or off.

</PageSection>

<PageSection>

## CSS custom properties

### Icon Button

| Name                                | Description
| :-----------------------------------| :------------------
| `--mdc-theme-primary`               | Controls the color of the icon when toggled on.
| `--mdc-theme-on-surface`            | Controls the background-color of the ripple.

### Badge

| Name                                | Description
| :-----------------------------------| :------------------
| `--forge-badge-border`                | Controls the border of the badge.
| `--forge-badge-max-width`             | Controls the max-width of the badge.
| `--forge-app-bar-background-color`    | Controls the color of the badge when included in the app-bar context.

</PageSection>

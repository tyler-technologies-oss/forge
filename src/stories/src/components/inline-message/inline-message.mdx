import { DefaultTemplate as InlineMessageDemo } from './templates/default';
import { InlineMessageDefaultCodeHtml } from './code/inline-message-default';
import { InlineMessageThemeCodeHtml } from './code/inline-message-theme';
import { InlineMessageIconCodeHtml } from './code/inline-message-icon';
import { InlineMessageTitleCodeHtml } from './code/inline-message-title';
import { InlineMessageIconTitleCodeHtml } from './code/inline-message-icon-title';

<PageSection>

# Inline message

The inline-message component provides a static, targeted message to the use. It has a moderate amount of emphasis but should not be used for page specific messaging.
If you need to show a page or app wide messages, you should consider using the [banner component](?path=/story/components-banner--default) instead.

This component supports built-in predefined themes.

</PageSection>

---

<PageSection>

## Example

### Basic

Below is the most basic usage of the inline message for displaying a static message:

<LiveDemo codeHtml={InlineMessageDefaultCodeHtml()}>
  <InlineMessageDemo />
</LiveDemo>

### Themed

You can use the built-in themes via attribute. If you need a custom color, see the CSS custom properties section below.

<LiveDemo layout="column" codeHtml={InlineMessageThemeCodeHtml()}>
  <InlineMessageDemo theme="danger" />
  <br/>
  <InlineMessageDemo theme="warning" />
  <br/>
  <InlineMessageDemo theme="success" />
  <br/>
  <InlineMessageDemo theme="info-primary" />
  <br/>
  <InlineMessageDemo theme="info-secondary" />
</LiveDemo>

### Combined with an icon

Here is an example usage combined with the an icon:


<LiveDemo codeHtml={InlineMessageIconCodeHtml()}>
  <InlineMessageDemo hasIcon={true}/>
</LiveDemo>

### Combined with a title

Here is an example usage combined with a title:


<LiveDemo codeHtml={InlineMessageTitleCodeHtml()}>
  <InlineMessageDemo hasTitle={true}/>
</LiveDemo>


### Combined with both an icon and a title

Here is an example usage combined with both an icon and a title:


<LiveDemo codeHtml={InlineMessageIconTitleCodeHtml()}>
  <InlineMessageDemo hasIcon={true} hasTitle={true}/>
</LiveDemo>

</PageSection>

---

<PageSection>

## Properties/Attributes

<PropertyDef name="theme" type="string" prop={false} defaultValue="&quot;info-primary&quot;">

Allows for easily setting the theme color to use. Valid values: `danger`, `warning`, `success`, `info-primary`, `info-secondary`.

</PropertyDef>

</PageSection>

---

<PageSection>

## Slots

| Name                           | Description
| :------------------------------| :----------------
| `default`                      | The default (unnamed) slot will render its content as the main text body.
| `icon`                         | The icon slot will render its content as the leading icon.
| `title`                        | The title slot will render its content above the main text body.

</PageSection>

<PageSection>

## CSS custom properties

| Name                                                  | Description
| :-----------------------------------------------------| :----------------
| `--forge-inline-message-theme-background`               | Sets the inline message's background color.
| `--forge-inline-message-theme-on-background`            | Sets the inline message's font color.
| `--forge-inline-message-theme-icon`                     | Sets the inline message's icon color.
| `--forge-inline-message-border-radius`                  | Sets the inline message's border radius.

</PageSection>
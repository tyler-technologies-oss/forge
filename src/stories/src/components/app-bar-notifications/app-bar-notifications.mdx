import { Default as AppBarNotificationsDemo } from './app-bar-notifications.stories';
import { AppBarNotificationButtonDefaultCodeHtml } from './code/app-bar-notifications-default';

<PageSection>

# App Bar Notifications

For convenience Forge provides the `<forge-app-bar-notification-button>` component. This component uses an icon-button
with a predefined icon. The power of this component is that it will ensure a consistent experience for users
across all applications that consume it.

If your application provides system notifications, use this component to show those notifications to the user.

> It is expected that you place this component in the `emnd` slot of the app-bar.

</PageSection>

---

<PageSection>

## Example

The notification button provides a consistent icon, as well as options to control an optional built-in badge.

<LiveDemo layout="column" fullWidth codeHtml={AppBarNotificationButtonDefaultCodeHtml()}>
  <AppBarNotificationsDemo />
</LiveDemo>

> In the future this component may have the ability to display notifications in a common format, but that functionality does not exist yet.

</PageSection>

---

<PageSection>

## Properties/Attributes

<PropertyDef name="count" type="number" defaultValue="0">

  Gets/sets the count value of the notification button.

</PropertyDef>

<PropertyDef name="dot" type="boolean" defaultValue="false">

  Gets/sets whether the badge shows dots.

</PropertyDef>

<PropertyDef name="showBadge" type="boolean" defaultValue="false">

  Gets/sets whether the notification button shows the badge.

</PropertyDef>

<PropertyDef name="theme" type="string" defaultValue="'secondary'">

  Gets/sets the theme of the badge.

</PropertyDef>

</PageSection>

---

<PageSection>

### CSS custom properties

See the [icon-button](?path=/docs/components-icon-button--default#css-custom-properties) component for information about which CSS custom properties are available.

</PageSection>

---

<PageSection>

## Accessibility

- The component will handle adding necessary ARIA attributes for you, as well as ensure the proper color contrast ratio is used.

</PageSection>

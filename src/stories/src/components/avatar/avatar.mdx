import { DefaultTemplate as AvatarDemo } from './templates/default';
import { AvatarImageCodeHtml, AvatarImageCodeBlazor } from './code/avatar-image';
import { AvatarLetterCodeHtml, AvatarLetterCodeBlazor } from './code/avatar-letter';

<PageSection>

## Usage

The avatar component allows you to provide text or images to display that represent an entity. By default, the avatar
will display textual content as single characters (character count is configurable), or display an image or icon based
on the URL provided to it.

</PageSection>

---

<PageSection>

## Example

### Avatar (image)

<LiveDemo 
dense={true} 
codeHtml={AvatarImageCodeHtml()} 
codeBlazor={AvatarImageCodeBlazor()}>
  <AvatarDemo 
  imageUrl={'https://en.gravatar.com/userimage/27084046/aa996f464ca8f1ea69769cef1b76fbf9.jpg'}
  text={''}/>
</LiveDemo>

### Avatar (letter)

<LiveDemo 
dense={true} 
codeHtml={AvatarLetterCodeHtml()} 
codeBlazor={AvatarLetterCodeBlazor()}>
  <AvatarDemo/>
</LiveDemo>

</PageSection>

---

<PageSection>

## Properties/Attributes

<PropertyDef name="autoColor" type="boolean" defaultValue="false">

Controls whether the background color is set automatically based on the text value. Does not have any effect when an image URL is specified.

</PropertyDef>

<PropertyDef name="imageUrl" type="string" defaultValue="undefined">

Sets the background image URL to use in place of the text representation.

</PropertyDef>

<PropertyDef name="letterCount" type="number" defaultValue="2">

Controls the number of characters to display when using textual content. Default is 2.

</PropertyDef>

<PropertyDef name="text" type="boolean" defaultValue="&quot;&quot;">

Gets/set the text to display as characters. If spaces exist, the first letter of each word will be used.

</PropertyDef>

</PageSection>

---

<PageSection>

## Slots

| Name               | Description
| :------------------| :--------------------
| `default`          | The default (unnamed) slot will override any content set by the component.

</PageSection>

---

<PageSection>

## CSS custom properties

| Name                                            | Description
| :-----------------------------------------------| :--------------------
| `--forge-avatar-theme-background`                 | Controls the `background-color` style.
| `--forge-avatar-font-size`                        | Controls the `font-size` style.
| `--forge-avatar-font-weight`                      | Controls the `font-weight` style.
| `--forge-avatar-theme-on-background`              | Controls the `color` of the font.
| `--forge-avatar-radius`                           | Controls the `border-radius` style. Can be useful for custom avatar shapes.
| `--forge-avatar-size`                             | Controls the `height` and `width` styles together.

</PageSection>
